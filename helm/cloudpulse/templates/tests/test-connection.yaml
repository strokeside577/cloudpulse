apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "cloudpulse.fullname" . }}-test-connection"
  labels:
    {{- include "cloudpulse.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "cloudpulse.fullname" . }}:{{ .Values.service.port }}/health']
  restartPolicy: Never